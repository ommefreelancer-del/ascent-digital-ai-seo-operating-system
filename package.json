{
  "name": "adasos-boss-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Boss Agent orchestration and routing framework for the Ascent Digital AI SEO Operating System (ADASOS).",
  "type": "module",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
